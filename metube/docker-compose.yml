networks:
  caddy:
    external: true

services:
  metube:
    container_name: metube
    image: ghcr.io/alexta69/metube
    hostname: metube
    networks:
      - caddy
    restart: unless-stopped
    volumes:
      - "${HOME}/downloads:/downloads"